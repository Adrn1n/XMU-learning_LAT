<!--
exercises
• write down a grammar which generates the 
following language {anb
n
|n<=4}. Is it a RG? a 
CFG?
• what is language generated by the following 
grammar?
{S → aA , A → Sb , S → ε }
is it a regular grammar?
• read 
http://en.wikipedia.org/wiki/Formal_grammar
 http://tcno.net/doc/tor/
•求句型i*i+i的短语和句柄
E
T
T
G[E]：E→E+T|T
 T→T*F|F
 F→(E)|i
句型：i*i+i
短语：
直接短语：
句柄：
T F
F
i * i + i
E
F
• 确定以下文法是否有二义性。如是，给
出理由。
 S → S + S | (S) | id
exercises
-->
- write down a grammar which generates the following language {anbn|n<=4}. Is it a RG? a CFG?
- what is language generated by the following grammar?{S → aA , A → Sb , S → ε }is it a regular grammar?
- read [http://en.wikipedia.org/wiki/Formal_grammar](http://en.wikipedia.org/wiki/Formal_grammar)
- 求句型i\*i+i的短语和句柄
	```mermaid
	graph TD
		E0["E"]
		E0 --> E1["E"]
		E0 --> OP1["\+"]
		E0 --> T1["T"]
		E1 --> T2["T"]
		T1 --> F1["F"]
		T2 --> T3["T"]
		T2 --> OP2["\*"]
		T2 --> F2["F"]
		F1 --> NUM1["i"]
		T3 --> F3["F"]
		F2 --> NUM2["i"]
		F3 --> NUM3["i"]
		
	```
	G\[E\]：E→E+T|T
	T→T\*F|F
	F→(E)|i
	句型：i\*i+i
	短语：
	直接短语：
	句柄：
- 确定以下文法是否有二义性。如是，给出理由。
	S → S + S | (S) | id 

---
- 
	$G = (V_t = \{a, b\}, V_n = \{A_1, A_2, A_3\}, S, P : \{S \to a A_1 b | \epsilon, A_1 \to a A_2 b | \epsilon, A_2 \to a A_3 b | \epsilon, A_3 \to a b | \epsilon\})$; No; Yes
- $\{a^n b^n\}$; No
- [http://en.wikipedia.org/wiki/Formal_grammar](http://en.wikipedia.org/wiki/Formal_grammar)
- 
	- 短语: i\*i+i, i\*i, i
	- 直接短语: i
	- 句柄: i
- 
	Yes
	```mermaid
	graph TD
	R0_1["S"]
	R0_2["S"]
	R0_1 --> N1_1["S"]
	R0_1 --> OP1_1["\+"]
	R0_1 --> N2_1["S"]
	R0_2 --> N1_2["S"]
	R0_2 --> OP1_2["\+"]
	R0_2 --> N2_2["S"]
	N1_1 --> T1_1["id"]
	N2_1 --> N3_1["S"]
	N2_1 --> OP2_1["\+"]
	N2_1 --> N4_1["S"]
	N1_2 --> N3_2["S"]
	N1_2 --> OP2_2["\+"]
	N1_2 --> N4_2["S"]
	N2_2 --> T1_2["id"]
	N3_1 --> T2_1["id"]
	N4_1 --> T3_1["id"]
	N3_2 --> T2_2["id"]
	N4_2 --> T3_2["id"]
	
	```
